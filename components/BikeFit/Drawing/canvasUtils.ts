/**
 * Legacy canvas utilities - maintained for backward compatibility
 *
 * @deprecated Use the modularized functions from the Drawing module instead:
 * - import { setupCanvas, drawSkeleton, drawBikeFitAngles } from './index'
 */

// Re-export all functions from the new modular structure for backward compatibility
export * from './index'
